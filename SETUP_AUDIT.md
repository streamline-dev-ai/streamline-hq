# Streamline Social — Setup Audit
**Generated:** 2026-05-27  
**Status:** READY TO RUN BATCH: **CONDITIONAL YES** — works now, but three silent breakages need fixing first (see BLOCKING below).

---

## 1. Folder Map

| Folder | Contents | Status |
|---|---|---|
| `.agents/skills/n8n-architect/` | n8n-as-code skill (SKILL.md) | ✅ loaded |
| `.claude/skills/streamline-social/` | Social skill (`clean-SKILL.md`) | ⚠️ **WRONG FILENAME** — see blocking |
| `.github/` | GitHub agents auto-generated by n8nac | — |
| `.n8nac/` | n8n-as-code workspace config | — |
| `All-Mcps/` | 25 mockup images, all clients combined | ✅ populated |
| `Assets/` | Subfolders: Ameli, BLOM, RecklessBear (desktop+mobile) | ✅ |
| `Assets/client-logos/` | **EMPTY** | ❌ missing |
| `Assets/logos/` | **EMPTY** — skill expects `streamline-dark.svg` here | ❌ missing |
| `Assets/photos/` | **EMPTY** — needed for BTS posts | ⚠️ optional |
| `Backgrounds/` | **EMPTY** | ⚠️ optional |
| `Best posts/` | 13 reference PNGs (numbered + about set) | ✅ populated |
| `Brand/` | 4 brand files (all prefixed `clean-*.md`) | ⚠️ **RENAMED — see blocking** |
| `Desktop Mcps/` | 4 Ameli desktop mockup images | ✅ |
| `Feed Posts/` | 3 carousel subfolders + 6 CTA PNGs | ✅ archive |
| `Final logos/` | Streamline-black.png, Streamline-White.png, SVG/ subfolder | ✅ populated |
| `Final logos/SVG/` | 7 SVG files (Logo V1_1, Name, favicons, etc.) | ✅ |
| `FInals/Highlight Cover/` | **EMPTY** | — |
| `Highlights/` | 5 category subfolders (About, Contact, Packages, Portfolio, Tips — some empty) | ⚠️ partial |
| `Inspo/` | 15 items: Canva templates, reference images, 2 reference HTML files | ✅ reference only |
| `Mcps/` | BLOM + Reckless mockup subfolders | ✅ |
| `Mobile MCps/` | 4 phone mockup images | ✅ |
| `node_modules/` | puppeteer installed | ✅ |
| `Output/` | 10 output batches (all HTML + PNG pairs — system has been used) | ✅ active |
| `Pinned Posts/` | 2 pin sets (4 PNGs each) + 2 standalone PNGs | ✅ archive |
| `Reels/` | 2 .mp4 walkthrough videos (Ameli + BLOM) | ✅ |
| `scripts/` | 12 JS files — screenshot + 11 generators | ✅ |
| `Site Screnshots Mobile/` | 4 client subfolders (BLOM, JJ glass, Ameli, Reckless) | ✅ |
| `Streamline Automation IG Static Posts/` | 9 PNGs (existing manual posts) | ✅ archive |
| `Streamline Automations IG Carousel Posts/` | 10 PNGs (existing manual posts) | ✅ archive |
| `Templates/example-posts/` | 1 HTML: `example-stat-hook-cw-electronics.html` | ✅ calibration anchor |

---

## 2. Installed Skills

| Skill | Location | Purpose |
|---|---|---|
| `streamline-social` | `.claude/skills/streamline-social/clean-SKILL.md` | Expert IG post/carousel/highlight generator for Streamline Automations — reads brand files, writes HTML, auto-screenshots to PNG |
| `n8n-architect` | `.agents/skills/n8n-architect/SKILL.md` | Full n8n-as-code workflow authoring, validation, sync, and push |

**⚠️ WARNING:** The `streamline-social` skill file is named `clean-SKILL.md`. Claude Code discovers skills by looking for `SKILL.md` exactly. This skill is NOT auto-loading. Confirmed: `streamline-social` does not appear in the active skills list at session start (compare: `streamline-site`, `demo-site-builder`, etc. all appear — `streamline-social` does not).

---

## 3. Missing Skills (Recommendations)

The current system has one multi-purpose social skill. It works, but for batch generation the following would be useful additions:

| Recommended Skill | Why Needed |
|---|---|
| `frontend-design` | Already installed globally — produces high-quality HTML components; useful as a fallback design engine if the social skill needs a nudge on layout quality |
| Carousel-batch skill | Not needed separately — the prompt library (28 archetypes) + social skill cover this, but a dedicated `generate-carousel` script matching the website generator pattern would remove friction |
| Static post composer | Not needed as a skill — `screenshot.js` + social skill handle it — but a `generate-static-posts.js` batch script (like the website generator series) would be useful |

**Verdict:** No new skill installs are blocking. The social skill covers everything. The real gap is the filename issue.

---

## 4. Brand Assets Inventory

### What's available

| Asset | Location | Format | Notes |
|---|---|---|---|
| Streamline logo (dark) | `Final logos/Streamline-black.png` | PNG | Used by all current scripts |
| Streamline logo (white) | `Final logos/Streamline-White.png` | PNG | Used by dark-bg slides |
| Streamline wordmark | `Final logos/SVG/Name.svg` | SVG | Pure name, no icon |
| Logo V1_1 | `Final logos/SVG/Logo V1_1.svg` | SVG | Full logo mark |
| Favicon variants | `Final logos/SVG/Favicon V2–V4.svg` | SVG | Icon-only variants |
| Logo colour variation | `Final logos/SVG/Logo Colour Variation.svg` | SVG | |
| Design system | `Brand/clean-design-system.md` | MD | Full spec — backgrounds, type scale, components |
| Voice guide | `Brand/clean-voice-guide.md` | MD | First-person SA tone, CTAs, find+replace |
| Design tokens | `Brand/clean-tokens.css` | CSS | CSS custom properties, utility classes |
| Prompt library | `Brand/clean-prompt-library.md` | MD | 28 post archetypes with full layout specs |
| Client site screenshots | `Site Screnshots Mobile/` | PNG | BLOM, JJ glass, Ameli, Reckless |
| Mockup images | `All-Mcps/` (25), `Mcps/`, `Mobile MCps/`, `Desktop Mcps/` | PNG | All clients covered |

### What's missing

| Missing | Impact | Fix |
|---|---|---|
| `Assets/logos/` is empty | **High** — skill HTML uses `../../Assets/logos/streamline-dark.svg` which breaks the `<img>` tag | Copy/symlink logo files in from `Final logos/` OR update skill to use `Final logos/` path |
| `Assets/client-logos/` is empty | **Medium** — skill references this path for client showcase posts | Copy client logos here |
| `streamline-dark.svg` doesn't exist at all | **High** — the skill's preferred logo path (`Assets/logos/streamline-dark.svg`) doesn't exist in any form | Export SVG from `Final logos/SVG/Logo V1_1.svg` or `Logo Colour Variation.svg` and rename |
| BTS photos in `Assets/photos/` | **Low** — only needed for BTS/personal posts | Add when needed |

### Brand file naming mismatch (BLOCKING)

The SKILL.md references:
- `Brand\design-system.md` → actual file is `Brand\clean-design-system.md`
- `Brand\voice-guide.md` → actual file is `Brand\clean-voice-guide.md`
- `Brand\prompt-library.md` → actual file is `Brand\clean-prompt-library.md`
- `Brand\tokens.css` → actual file is `Brand\clean-tokens.css`

When Claude Code runs the skill and tries to read these files, it will fail silently (file not found) and generate posts without brand context, producing off-brand output.

---

## 5. Sample Patterns Observed

### How the skill generates posts
1. Reads `Brand/*.md` files for design system + voice
2. Reads `Templates/example-posts/*.html` as a calibration anchor
3. Generates self-contained HTML with inline Google Fonts + inline CSS
4. Canvas: `1080×1350px` for feed posts, `1080×1920px` for stories/highlights
5. Saves to `Output/YYYY-MM-DD_<post-type>_<slug>/post.html` (or `slide-N.html`)
6. Runs `node scripts/screenshot.js Output/<folder>` → PNG next to HTML
7. Reports back with file paths + optional caption

### Template calibration example (`example-stat-hook-cw-electronics.html`)
- Demonstrates correct density: soft purple gradient bg, two visible corner blobs, grid overlay
- Hero stat at 400px (fills canvas), supporting lines at 72–96px
- Chips with glass backgrounds + purple dots
- Instrument Serif italic caption at 38px
- Wordmark fallback (purple dot + "STREAMLINE" text) since `Assets/logos/` was empty
- Layout: padding 70px/80px, all content in flex column, z-index layering (blobs z:1, grid z:2, content z:3)

### Generator scripts (`generate-websites-v4.js` pattern)
- Node.js, writes HTML strings directly using template literals
- References `../../Final logos/Streamline-black.png` (not `Assets/logos/`) for logos
- Outputs multiple variant folders (websites-v4-a, v4-b, v4-c) in one run
- `shoot-stories.js` handles batch screenshotting of nested folder trees
- All generators produce HTML + use `screenshot.js` or `shoot-stories.js` for PNG export

### Output/ — active history
| Batch | Slides | Type |
|---|---|---|
| `2026-05-13_stat-hook_cw-700-products` | 1 | Stat-led single post (the calibration post) |
| `services-websites`, `v2`, `websites-v4-a/b/c`, `websites-v4-a-r` | 6 slides each (5 carousel + 1 single) | Web services carousel (6 versions) |
| `highlights`, `highlights-v2` | 24 covers each (8 topics × 3 variants + welcome) | Highlight covers |
| `highlight-stories` | ~80 PNGs | IG story variants (6 topics × 3 vars × 4–5 slides) |

---

## 6. Content Hub HTML Status

**Result: FETCH FAILED — content exceeds 10MB limit.**

The URL `https://api.anthropic.com/v1/design/h/daV67zUcqbhjLWTXeEeGAg?open_file=Streamline+Content+Hub.html` returned a content-too-large error (WebFetch max: 10MB). The file was **not** saved to `Templates/`.

**What this likely is:** A large single-page HTML application (>10MB suggests bundled JS/CSS + possibly embedded assets). Given the URL pattern and filename (`Streamline Content Hub.html`), it's likely a custom-built content planning dashboard or post preview browser — probably generated in a previous session.

**How to retrieve it:** The URL looks like a Claude artifact share link. Open it directly in a browser to view and save it manually. Once saved, drop it at `Templates/streamline-content-hub.html` and note its actual purpose.

---

## 7. READY TO RUN BATCH: CONDITIONAL YES

The Puppeteer pipeline works (node_modules installed, scripts functional, Output/ has successful output). The system HAS been used and produces correct results.

### Blocking issues (fix before running the skill via chat prompt)

**Priority 1 — Skill won't load (silent breakage)**
- **Problem:** `.claude/skills/streamline-social/clean-SKILL.md` → Claude Code discovers skills named `SKILL.md` exactly. File must be renamed.
- **Fix:** Rename `clean-SKILL.md` → `SKILL.md`

**Priority 2 — Brand files won't be read (skill generates off-brand posts)**
- **Problem:** Skill reads `Brand\design-system.md` etc. but files are `Brand\clean-design-system.md`.
- **Fix (Option A):** Update all file references inside `SKILL.md` to match `clean-*.md` names.
- **Fix (Option B):** Rename `clean-*.md` files back to `design-system.md`, `voice-guide.md`, `prompt-library.md`, `tokens.css`.

**Priority 3 — Logo path broken (posts fall back to text wordmark)**
- **Problem:** Skill expects `Assets/logos/streamline-dark.svg` — folder is empty, file doesn't exist.
- **Fix:** Copy `Final logos/Streamline-black.png` → `Assets/logos/streamline-dark.png` and update the `<img>` path in skill, OR copy the SVG from `Final logos/SVG/Logo V1_1.svg` → `Assets/logos/streamline-dark.svg`.

### Non-blocking (nice to have)

| Issue | Impact |
|---|---|
| `Assets/client-logos/` empty | Showcase posts fall back to text labels — still works, just no client logos in post |
| `Backgrounds/` empty | Not referenced by current skill — no impact |
| `FInals/Highlight Cover/` empty | Archiving only — no impact |
| Some `Highlights/` subfolders empty (Portfolio, Tips) | Archive folders, not used by skill |

---

## Summary

The content generation system is **well-built and previously working** — Output/ shows 10 successful batches. The three blocking issues above are all naming/path problems introduced during a "clean" refactor of the brand files. Once fixed (estimated 5 minutes), the skill will load correctly and have access to all brand context and logo assets.

**Recommended fix order:**
1. Rename `clean-SKILL.md` → `SKILL.md`
2. Update skill's brand file references to match `clean-*.md` names (or rename the brand files to drop the prefix)
3. Copy a logo file into `Assets/logos/`
4. Restart Claude Code session
5. Test with: *"Make a hook post about how I build custom websites"*
